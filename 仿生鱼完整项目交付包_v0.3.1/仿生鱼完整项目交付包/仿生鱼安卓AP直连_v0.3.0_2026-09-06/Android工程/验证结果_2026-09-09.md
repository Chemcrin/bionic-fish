# Android 0.3.2 验证结果（2026-09-09）

对应本轮固定低速开/停的未提交工作区源码。旧0.3.1产物不替代本次验证。

## 结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 源码与构建副本 | 57个输入SHA-256全部匹配 | app/build/verification/validation.json |
| 完整构建 | testDebugUnitTest lintDebug assembleDebug assembleDebugAndroidTest 成功 | app/build/verification/build-and-checks.log |
| JVM单元测试 | 56/56通过，0失败、0错误、0跳过 | app/build/test-results/testDebugUnitTest/ |
| lint | 0错误，10条依赖版本更新提示 | app/build/verification/lint-results-debug.html |
| MuMu完整设备测试 | 23/23通过，0跳过，31.908秒 | app/build/verification/instrumentation-full.log |
| 冷启动 | 已安装0.3.2/code5，am start -W返回Status: ok | app/build/verification/startup.log |
| 控制行为 | 前进固定低速、停止和舵机可用，取消RPM选择与参数确认阻挡 | 协议、Repository、TCP loopback及UI测试 |
| 状态协议 | RPM可为NA，step_on表示驱动命令；缺失状态保持未知；兼容旧数字RPM | AsciiProtocolTest及设备测试 |
| 显示 | 浅/深色、200%字体及滚动停止按钮通过；已查看新截图 | app/build/verification/screenshots/renders/ |

使用用户现有MuMu实例1（Android15/API35，emulator-5556），通过官方ADB执行完整AndroidJUnitRunner，没有按类或用例过滤：

```text
adb -s emulator-5556 install -r app/build/outputs/apk/debug/app-debug.apk
adb -s emulator-5556 install -r app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk
adb -s emulator-5556 shell am instrument -w -r com.bionicfish.controller.test/androidx.test.runner.AndroidJUnitRunner
```

本次没有重新运行Gradle connected任务；设备测试为上述ADB执行结果。TCP用例使用生产TcpTransport/Repository和本机TCP端口上的生产Mock，证明软件交互，不是实际ESP AP验收。

## 产物

- APK：`app/build/outputs/apk/debug/app-debug.apk`
- 包名：`com.bionicfish.controller`；版本0.3.2，versionCode5；minSdk26、target/compileSdk36。已用aapt核对实际APK。
- 大小：20,389,118字节。
- SHA-256：`f7d4c3f7c27da8266b9f63f99b1a5d6e40bafbce52a78f1cec1d962154217718`。
- 调试签名，用于开发联调，未发布新Release。

构建工具为Gradle8.13、AGP8.13.2、Kotlin2.3.21、Build Tools36.0.0；Java/Kotlin目标17，构建JDK21.0.7。Windows使用短ASCII构建目录，输入文件与交付目录逐一核对。当前APK必须与支持NA RPM/step_on的本轮固件配套使用。

## 实板边界

ESP AT1.7.4和当前115200/8N1/无流控参数已由用户回包确认。STM32默认允许TB6612以10%PWM、每50ms换相连续运行；没有位置/RPM控制或参数确认门禁。真实转动与温升、STM32到ESP通信、OLED和JY61P仍待实板联调，模拟器测试不能替代这些结果。
