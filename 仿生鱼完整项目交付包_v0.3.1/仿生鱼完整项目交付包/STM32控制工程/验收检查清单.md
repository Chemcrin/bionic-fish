# 当前交付验收清单

当前范围：STM32 + ESP-01S AT/TCP + Android；TB6612 推进固定低速开/停。按键不启用，OLED 保留单页布局，HarmonyOS 暂停。

## 软件验证

**本轮结果：17/17 CTest、Arm固件构建、56/56 Android单元测试、23/23 MuMu设备测试全部通过，lint 0错误。** BIN为24,604字节；以下列出验证范围及证据。

| 检查 | 判据 |
| --- | --- |
| C 宿主回归 | python scripts/build.py host --host-cc D:/mingw64/bin/gcc.exe 全部通过；日志在 build-host/Testing/Temporary/LastTest.log |
| 固定低速推进 | 默认前进允许连续四拍换相、PWM10%、周期50ms；旧60/100字段均不改变速度；保活和调舵不重置节拍 |
| 停止与禁用 | STOP、失联、UART丢失及发送失败释放两相；显式禁用配置拒绝前进 |
| 协议与链路 | 严格分帧、同序号幂等、逐命令AT响应、实际连接ID、接收损坏恢复、发送结果核查、旧会话隔离 |
| 姿态与屏幕 | 错误/过期姿态不可用；OLED缺失不阻止握手，恢复后继续原单页信息显示 |
| Arm 构建 | 真实HAL/CMSIS及业务源码在Werror下交叉构建；BIN/ELF/MAP大小和哈希见build-firmware/build-info.json |
| 烧录脚本 | 6项模拟调用检查通过：连接模式、频率、BIN/ELF地址、空格路径和失败退出；记录见build-firmware/flash-wrapper-validation.json，未实际烧录 |
| Android 构建 | testDebugUnitTest lintDebug assembleDebug assembleDebugAndroidTest 成功；支持RPM为NA及step_on状态 |
| MuMu 测试 | 完整AndroidJUnitRunner通过；前进/停止、舵机、状态解析、TCP软件闭环和控制页操作验证 |

Android当前版本0.3.2/code5，证据见其[验证记录](../仿生鱼安卓AP直连_v0.3.0_2026-09-06/Android工程/验证结果_2026-09-09.md)。使用官方ADB直接运行完整instrumentation APK，不将未完成的Gradle connected任务记为成功。

## 已有硬件信息

- 用户提供ESP AT1.7.4.0/SDK3.0.5-dev回包；修正后的当前UART为115273,8,1,0,0，匹配本工程115200/8N1/无流控。
- 无铭牌42步进电机，两组线圈已用万用表配对并接入TB6612 M1/M2。报告约3Ω，VM约12V，来自45C、3S航模锂电。
- 用户已选择保留TB6612，以10%PWM、固定低速试运行，不再以铭牌、每转步数或参数确认标志阻止前进。10%不是已测相电流或恒流保护。

## 实板联调

| 操作 | 预期结果 | 证据状态 |
| --- | --- | --- |
| 烧录并启动STM32/ESP | USART3诊断server=1，出现BionicFish-AP，TCP9000可连接 | 待实板 |
| 安卓AP直连 | STOP收到同序号ACK及link=1 STA，页面进入已连接状态 | 待实板 |
| 前进后持续保活 | 电机低速连续转动；step_on=1；观察方向、稳定性和温升 | 待实板 |
| 停止、再次前进 | 停止时两相释放、step_on=0；再次前进恢复低速 | 待实板 |
| 调整舵机 | 舵机响应且不打断推进节拍，行程符合实际机构 | 待实板 |
| 退出App/断Wi-Fi/拔ESP | 失联停止，不恢复旧推进目标；重新连接先STOP握手 | 待实板 |
| 查看和恢复传感器/屏幕 | 姿态有效时正常显示；无效时NA/---；屏幕原布局能持续刷新 | 待实板 |

软件测试和模拟器不能证明实物电机、传感器及无线链路已正常工作。实板联调结果确认后，才能将整个工程正常使用标为完成。
